---
export interface Props {
  date: Date
}

const { date } = Astro.props
---

<time class="text-gray-400 mr-5" datetime={date.toISOString()}>
  {
    date.toLocaleDateString('en-us', {
      day: 'numeric',
      month: 'short',
      year: 'numeric'
    })
  }
</time>
